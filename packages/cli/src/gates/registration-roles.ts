/**
 * THE registration-role measurement's shape — re-exported. Round 13 (P4): it
 * moved to `@shrkcrft/boundaries`
 * (`packages/boundaries/src/wiring/i-registration-roles.ts`) with
 * `measureRegistrationRoles`, and gained the `unitAcceptance?` carrier (settle
 * record B). Edit it there.
 */
export type { IRegistrationRoles } from '@shrkcrft/boundaries';
