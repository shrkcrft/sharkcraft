/**
 * The knowledge-entry fields that declare checkable claims — what a malformed
 * claim ({@link IMalformedKnowledgeClaim}) was declared under.
 */
export enum KnowledgeClaimField {
  References = 'references',
  Anchors = 'anchors',
}
