export enum OverwriteStrategy {
  Never = 'never',
  Ask = 'ask',
  Overwrite = 'overwrite',
  MergeLater = 'merge-later',
}

export enum NamingStrategy {
  AsIs = 'as-is',
  Kebab = 'kebab',
  Pascal = 'pascal',
  Camel = 'camel',
  Snake = 'snake',
}
