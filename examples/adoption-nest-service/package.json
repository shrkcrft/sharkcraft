{
  "name": "adoption-nest-service",
  "version": "0.1.0",
  "description": "R47 adoption fixture — NestJS service.",
  "scripts": {
    "build": "nest build",
    "start": "nest start",
    "test": "jest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@nestjs/core": "^10.0.0",
    "@nestjs/common": "^10.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "jest": "^29.0.0"
  }
}
