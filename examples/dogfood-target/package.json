{
  "name": "@example/dogfood-target",
  "version": "0.1.0",
  "description": "Realistic TypeScript service skeleton used to dogfood SharkCraft against an external-feeling repo.",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "bun run src/server.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@shrkcrft/config": "workspace:*",
    "@shrkcrft/knowledge": "workspace:*",
    "@shrkcrft/rules": "workspace:*",
    "@shrkcrft/paths": "workspace:*",
    "@shrkcrft/templates": "workspace:*",
    "@example/sharkcraft-pack-example": "workspace:*"
  }
}
