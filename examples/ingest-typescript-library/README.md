# ingest-typescript-library

Publishable TypeScript library fixture. One exported interface, one exported factory, strict mode. Use with `shrk ingest repository --preset strict-typescript --preset npm-package`.
