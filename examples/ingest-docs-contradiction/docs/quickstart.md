# Quickstart

> Note: the CLI used to be called `sharkcraft`; it's `shrk` now.

Run the build:

```bash
bun run build
```

The build script is defined in `packages/missing/build.ts`.

Then start the server:

```bash
sharkcraft serve
```

The legacy entrypoint lives at `src/sharkcraft.plugin.ts`.
