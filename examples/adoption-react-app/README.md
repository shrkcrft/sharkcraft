# adoption-react-app

R47 adoption fixture. A minimal React SPA — `react` + `react-dom` +
`vite`, `vitest` for tests, `eslint` configured at the package level.
Used to verify that `shrk init --zero-config` picks `react-app` for a
React project.
