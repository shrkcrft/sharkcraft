# Angular-style app — SharkCraft example

Minimal example showing what an Angular-flavoured `sharkcraft/` folder looks like.
The example is **not** a real Angular app — just the SharkCraft knowledge tuned
for Angular conventions.
