# ingest-angular-modern

Fixture for `shrk ingest repository --preset modern-angular`.

Convention: signals-first state, OnPush components, lazy routes. Implicit rule: never import `@angular/animations` from domain code.
