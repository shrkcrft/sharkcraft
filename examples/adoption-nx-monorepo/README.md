# adoption-nx-monorepo

R47 adoption fixture. Nx-flavored monorepo markers (`nx.json`,
`workspaces`). No real workspace targets — the fixture exists to
verify `shrk init --zero-config` picks `nx-monorepo`.
